/**
 * Name of the platform that users will use to register the plugin in the Homebridge config.json
 */
export const PLATFORM_NAME = 'ModbusCustom';

/**
 * Name of plugin as defined the package.json
 */
export const PLUGIN_NAME = 'homebridge-modbus-custom';